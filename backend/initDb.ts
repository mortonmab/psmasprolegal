import dotenv from 'dotenv';

dotenv.config({ path: '../../.env' });

console.log('Firestore (Firebase) is schemaless. No database initialization required.');
// No further action needed for Firestore setup.
